"""
Blackboard Learn SDK for Python.

Auto-generated by blackboard-learn-sdk-generator v{{metadata.generatorVersion}}
"""

from .client import Client
from ._auth import OAuth2Client, AuthConfig
from ._pagination import paginate, SyncPage, AsyncPage
from ._errors import (
    APIError,
    AuthenticationError,
    PermissionError,
    NotFoundError,
    BadRequestError,
    ConflictError,
    RateLimitError,
    InternalServerError,
    ServiceUnavailableError,
)

# Resource classes
{{#each topLevelResources}}
from .resources.{{snakeCase name}} import {{PascalCase name}}Resource
{{/each}}

# Type exports
{{#each models}}
from .types.{{snakeCase originalName}} import {{name}}
{{/each}}

{{#each enums}}
from .types.{{snakeCase name}} import {{name}}
{{/each}}

__all__ = [
    "Client",
    "OAuth2Client",
    "AuthConfig",
    "paginate",
    "SyncPage",
    "AsyncPage",
    # Errors
    "APIError",
    "AuthenticationError",
    "PermissionError",
    "NotFoundError",
    "BadRequestError",
    "ConflictError",
    "RateLimitError",
    "InternalServerError",
    "ServiceUnavailableError",
    # Resources
{{#each topLevelResources}}
    "{{PascalCase name}}Resource",
{{/each}}
    # Models
{{#each models}}
    "{{name}}",
{{/each}}
    # Enums
{{#each enums}}
    "{{name}}",
{{/each}}
]
